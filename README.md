# API Gateway Example in AWS CDK 

creating an API Gateway with Lambda integration

sample endpoints:
`/todos` with HTTP `GET`
`/todos/{todoId}` with HTTP `DELETE`

<!-- image -->
![image](/src/assets/output.PNG)
